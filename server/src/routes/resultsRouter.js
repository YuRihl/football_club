import { Router } from "express";
const router = Router();

import {  } from '../controllers/resultsController.js';

router.get('/', );
router.get('/:id', );

router.post('/', );
router.put('/:id', );
router.delete('/:id', );

export default router;